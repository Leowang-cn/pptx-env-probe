# 版本上限来自部署后台「环境信息 → 依赖版本查询」实测（2026-09-23）。
# 该机器从内网镜像装包，且 gcc 4.8.5 / glibc 2.17，需现场编译的依赖装不上。
# 改动此文件前，请先在后台查询确认目标版本可安装，不要照抄官方最新版。
--only-binary=:all:
fastapi==0.141.1
uvicorn==0.53.0
python-pptx==1.0.2
lxml==6.1.3
Pillow==12.2.0
PyMuPDF==1.26.0
SQLAlchemy==2.0.54
redis==8.1.0
minio==7.2.20
